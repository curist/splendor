import assert from 'assert';

import { playerBoughtCard } from '../app/AI/helpers';


describe('AI helpers', () => {
  describe('playerBoughtCard', () => {
    it('should return original player if player can\'t buy the card', () => {
      assert(1 == 1);
      assert(1 == 1);
    });
  });
});


